# Postgres
DATABASE_URL="postgresql://trayex:trayexpass@localhost:5432/trayex?schema=public"

# JWT for API sessions
JWT_SECRET="dev-super-secret-change-me"

# QR key rotation (comma-separated secrets, latest first)
QR_JWT_KEYS="k3_latest_secret,k2_old_secret,k1_older_secret"

# CORS allowed origins (comma-separated)
CORS_ORIGINS="http://localhost:3000"

# Port
PORT=4000
